# Legacy Watchlist Archive

This directory is a historical archive of the old root-level `watchlist/` tree.

Rules:
- Do not use this directory as a runtime input source.
- The only active watchlist source is `data/watchlist/active/{code}/`.
- `000737` has been migrated back into `data/watchlist/active/` because it is still referenced by current rules and prediction summaries.
- `601933` and `601988` remain archived because their legacy profiles only contain empty template content and they are not part of the current active watchlist configuration.
