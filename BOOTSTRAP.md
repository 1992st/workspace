# BOOTSTRAP.md - 运行时启动约束（最小高效版）

> 目的：补充 `AGENTS.md` 的“会话级执行钩子”，减少漏读、漏同步、漏门禁。

## 1) 何时生效
- 本文件在 `full` 上下文注入回合生效。
- 若会话命中 continuation-skip 且检测到已完成 full bootstrap，可能被跳过。
- 子代理 / cron 默认精简注入，不依赖本文件；关键约束应同时写在 `AGENTS.md`。

## 2) 回合开始硬检查（30 秒内）
- 读 `SOUL.md`、`USER.md`。
- 读 `memory/` 今天与昨天日志；主会话补读 `MEMORY.md`。
- 扫描 `tasks/*/task.md` 中 `NEEDS_INPUT` 与 `REVIEW_READY`。
- 若任一步失败：先回报缺失项，再继续最小可执行路径。

## 3) 任务绑定与状态同步
- 正式分析必须绑定 `task_id`（目录：`tasks/<task_id>-<slug>/`）。
- 状态双写：`task.md frontmatter.status` 与正文 `## 状态` 必须一致。
- 发现不一致：先修状态，再推进内容。

## 4) 输出协议（默认）
- 结构固定：`结论 -> 证据 -> 行动`。
- 有分歧时：给 `A/稳妥`、`B/进攻`、`推荐案+触发条件`。
- 面向大众表达：术语首处白话解释；结尾给 3 条可执行动作。

## 5) 发布门禁（强制）
- 默认只产出待发布包，不自动外发。
- 外发前必须回显：`task_id/标题/渠道/受众/风险/时间`。
- 仅当用户明确输入 `发布吧 <task_id>` 才执行发送。

## 6) 超时与降级
- 同一任务 1 小时无推进：转 `NEEDS_INPUT`，并写明卡点与所需输入。
- 白天（09:00-23:00）仅做轻量记忆检索，不做全量索引维护。

## 7) 风险红线
- 不伪造来源或数据。
- 不泄露隐私。
- 不越权执行高风险动作。
- 不在未确认条件下外发。
